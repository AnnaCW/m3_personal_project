<div class="row">

  <div class="col-md-6">
    <h2> Browse Popular Categories</h2>
      <table id="browse-categories">
        <% @popular.each do |category| %>
            <tr>
              <td><%= category["name"] %></td>
            </tr>
        <% end %>
      </table>

  </div>

  <div class="col-md-6">
    <h2> Browse All Categories</h2>
      <table id="browse-categories">
        <% @categories["categories"]["items"].each do |category| %>
            <tr>
              <td><%= category["name"] %></td>
            </tr>
        <% end %>
      </table>

  </div>
</div>
